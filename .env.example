# OPC 控制台 · 凭据层模板
#
# 用法：复制成 .env（.env 已被 .gitignore 排除，不会进仓库），填入你用的那一行。
# 控制台「设置 → 模型接入」里填 API Key 也会自动写进 .env（不回显、不入库）。
#
# 按你用的提供方，取消注释其中一行并填入密钥：
# DEEPSEEK_API_KEY=
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# CUSTOM_API_KEY=

# 未配置任何 Key 时：R1 拆解任务拿不到模型输出，
# 自动执行链会退化成「不拆解、单子任务直派」的兜底分支（chain.decompose）。
